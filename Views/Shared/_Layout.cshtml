<!DOCTYPE html>
<html>
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - PTS </title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
    @Styles.Render("~/bundles/datatables")
</head>
<body>
    <div class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                @Html.ActionLink("Control de acceso", "Index", "HikVision", new { area = "" }, new { @class = "navbar-brand" })
            </div>

            <div class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    <li>@Html.ActionLink("Planteles", "Index", "Plantel")</li>
                    <li>
                        @using (Html.BeginForm("MainDashboard", "HikVision", FormMethod.Post, new { @class = "navbar-form", @style = "padding:0; margin:0;" }))
                        {
                            @Html.Hidden("APIServer", "http://192.168.0.5")
                            @Html.Hidden("Usuario", "admin")
                            @Html.Hidden("Password", "DS-K1T320")
                            <button type="submit" class="btn btn-link" style="padding: 15px 15px; color: #9d9d9d; text-decoration: none;">Main DS-K1T320</button>
                        }
                    </li>
                    @*<li>@Html.ActionLink("Dashboard 2", "Index", "HikVision")</li>
                    <li>@Html.ActionLink("Dashboard 3", "Index", "HikVision")</li>*@
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Otras <span class="caret"></span></a>
                        <ul class="dropdown-menu">                            
                            <li>@Html.ActionLink("Puntos de Acceso", "Index", "PuntosAcceso")</li>
                            <li>@Html.ActionLink("Tutores", "Index", "Tutor")</li>
                            <li>@Html.ActionLink("Estudiantes", "Index", "Estudiantes")</li>
                            <li role="separator" class="divider"></li>
                            <li>@Html.ActionLink("Usuarios", "Index", "User")</li>
                            <li>@Html.ActionLink("Privilegios", "Index", "Roles_and_Privilege")</li>
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </div>
    <div class="container body-content">
        @RenderBody()
        <hr />
        <footer>
            <p>&copy; @DateTime.Now - Developed by Julian Mondragon - Version V1.4</p>
        </footer>
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @Scripts.Render("~/bundles/datatables")
    @Scripts.Render("~/bundles/corecubi")
    @RenderSection("scripts", required: false)


</body>
</html>
    <script src="~/Scripts/helper.js"></script>
    <script type="text/javascript">
        // Intervalo en milisegundos (aquí configurado para cada 4 minutos)
        var keepAliveInterval = 4 * 60 * 1000;

        function keepSessionAlive() {
            let currentUrl = window.location.href; // Captura la URL actual

            fetch(`/Home/KeepAlive?currentUrl=${encodeURIComponent(currentUrl)}`, {
                method: 'GET',
                cache: 'no-cache'
            })
                .then(response => {
                    if (!response.ok) {
                        console.error('Error manteniendo la sesión activa:', response.statusText);
                    }
                })
                .catch(error => console.error('Error de red al intentar mantener la sesión activa:', error));
        }
        // Configura el intervalo para mantener la sesión activa
        setInterval(keepSessionAlive, keepAliveInterval);
    </script>

</body>    
</html>
